import deviceInfo from './device';
import {
  getUrlParams,
  isExternal,
  formatSecond,
} from './utils';

export {
  deviceInfo, // 获取设备信息
  getUrlParams, // 获取 url 中的 query 参数
  isExternal, // 判断是否是外部链接
  formatSecond, // 格式化时分秒
};